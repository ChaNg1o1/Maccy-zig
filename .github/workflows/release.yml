name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, for example v0.1.0"
        required: true
        type: string
      prerelease:
        description: "Mark this release as a prerelease"
        required: true
        default: true
        type: boolean

permissions:
  contents: write

jobs:
  build-release:
    name: Build and publish macOS app
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Install packaging dependencies
        run: brew install imagemagick

      - name: Resolve release metadata
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            release_tag="${{ inputs.tag }}"
            prerelease="${{ inputs.prerelease }}"
          else
            release_tag="${GITHUB_REF_NAME}"
            prerelease="false"
          fi

          if [[ ! "$release_tag" =~ ^v[0-9]+(\.[0-9]+){0,2}([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Release tag must look like v0.1.0, got: $release_tag" >&2
            exit 1
          fi

          echo "RELEASE_TAG=$release_tag" >> "$GITHUB_ENV"
          echo "RELEASE_VERSION=${release_tag#v}" >> "$GITHUB_ENV"
          echo "PRERELEASE=$prerelease" >> "$GITHUB_ENV"
          echo "ZIP_PATH=dist/MaccyZig-${release_tag}-macOS.zip" >> "$GITHUB_ENV"
          echo "SHA_PATH=dist/MaccyZig-${release_tag}-macOS.zip.sha256" >> "$GITHUB_ENV"

      - name: Run tests
        run: zig build test

      - name: Package app
        run: VERSION="$RELEASE_VERSION" ./scripts/package-app.sh

      - name: Verify packaged app
        run: ./scripts/smoke-package.sh
        env:
          APP: dist/MaccyZig.app

      - name: Create downloadable archive
        shell: bash
        run: |
          set -euo pipefail
          ditto -c -k --keepParent "dist/MaccyZig.app" "$ZIP_PATH"
          shasum -a 256 "$ZIP_PATH" > "$SHA_PATH"

      - name: Write release notes
        shell: bash
        run: |
          set -euo pipefail
          checksum="$(cut -d ' ' -f 1 "$SHA_PATH")"
          cat > dist/release-notes.md <<EOF
          \`\`\`text
          __  __                         ______ _
          |  \/  | __ _  ___ ___ _   _  |__  /(_) __ _
          | |\/| |/ _\` |/ __/ __| | | |   / / | |/ _\` |
          | |  | | (_| | (_| (__| |_| |  / /_ | | (_| |
          |_|  |_|\__,_|\___\___|\__, | /____|/ |\__, |
                                  |___/      |__/ |___/
          \`\`\`

          ## MaccyZig ${RELEASE_TAG}

          A native macOS clipboard manager built with Zig and AppKit.

          ## Download

          Download \`MaccyZig-${RELEASE_TAG}-macOS.zip\` from this release, unzip it, then move \`MaccyZig.app\` to \`/Applications\`.

          ## First launch (required — the app is not notarized)

          This build is ad-hoc signed, so macOS Gatekeeper will refuse to open
          it with an "Apple could not verify" dialog. To open it the first time:

          1. Double-click \`MaccyZig.app\` once (the dialog appears — close it).
          2. Open **System Settings → Privacy & Security**, scroll down, and
             click **Open Anyway** next to the MaccyZig message.
          3. Confirm in the dialog that follows. This is only needed once.

          Alternatively, from a terminal:

          \`\`\`sh
          xattr -d com.apple.quarantine /Applications/MaccyZig.app
          \`\`\`

          ## Verification

          SHA-256:

          \`\`\`text
          ${checksum}
          \`\`\`

          ## Requirements

          - macOS 14 or newer (universal: Apple Silicon + Intel)
          - Accessibility permission is required for auto-paste (the app will
            guide you on first paste)
          EOF

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: MaccyZig-${{ env.RELEASE_TAG }}-macOS
          path: |
            ${{ env.ZIP_PATH }}
            ${{ env.SHA_PATH }}
            dist/verification/*

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          args=(
            "$RELEASE_TAG"
            "$ZIP_PATH#MaccyZig-${RELEASE_TAG}-macOS.zip"
            "$SHA_PATH#MaccyZig-${RELEASE_TAG}-macOS.zip.sha256"
            --title "MaccyZig ${RELEASE_TAG}"
            --notes-file dist/release-notes.md
            --target "$GITHUB_SHA"
          )
          if [ "$PRERELEASE" = "true" ]; then
            args+=(--prerelease)
          fi
          # Idempotent: re-running the job for an existing release replaces
          # its assets instead of failing and leaving a half-published state.
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "$ZIP_PATH" "$SHA_PATH" --clobber
            gh release edit "$RELEASE_TAG" --notes-file dist/release-notes.md
          else
            gh release create "${args[@]}"
          fi
